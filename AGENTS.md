# エージェント運用ルール（LyraCopyMVP）

## 言語
- すべての返答は日本語で行うこと。
- ファイル名やコード、コマンドはそのまま英数字で表記してよい。

## スコープ
- この AGENTS.md の適用範囲は `LyraCopyMVP/` 配下のすべてのファイル。

## 変更方針（抜粋）
- 既存のコードスタイルを尊重し、変更は最小限に留める。
- 必要に応じてビルド・実行コマンドを提示する。
- UI 変更時は影響範囲を明示し、動作確認方法を併記する。
